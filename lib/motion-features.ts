// Loaded asynchronously by LazyMotion so animation code stays out of the
// initial bundle. Layout animations (domMax) are only loaded where used.
export { domAnimation as default } from "motion/react";
